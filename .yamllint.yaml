extends: relaxed

rules:
  line-length:
    max: 120
  indentation:
    spaces: 2
  comments:
    min-spaces-from-content: 1
  comments-indentation: disable
  truthy:
    allowed-values: ['true', 'false', 'on', 'off']

ignore: |
  charts/*/charts/
  charts/*/Chart.lock
  charts/*.tgz
  .devspace/
